---
phase: 13-export-e-compartilhamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260410200000_create_shared_insights.sql
  - src/app/api/insights/share/route.ts
  - src/app/api/insights/share/[shareId]/route.ts
  - src/app/api/share/[token]/route.ts
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03]

must_haves:
  truths:
    - "Tabela shared_insights existe com token, generation_id, user_id, expires_at, revoked_at"
    - "POST /api/insights/share cria link (agency only) retornando token e expires_at"
    - "DELETE /api/insights/share/[shareId] revoga link (apenas dono)"
    - "GET /api/share/[token] retorna dados do insight sem PII se link válido"
    - "Link expirado ou revogado retorna 410 com mensagem 'link não disponível'"
  artifacts:
    - path: "supabase/migrations/20260410200000_create_shared_insights.sql"
      provides: "Tabela shared_insights para links públicos"
      contains: "CREATE TABLE shared_insights"
    - path: "src/app/api/insights/share/route.ts"
      provides: "Criação de link compartilhável (agency)"
      exports: ["POST"]
    - path: "src/app/api/insights/share/[shareId]/route.ts"
      provides: "Revogação de link"
      exports: ["DELETE"]
    - path: "src/app/api/share/[token]/route.ts"
      provides: "Endpoint público para visualizar insight por token"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/insights/share/route.ts"
      to: "supabase"
      via: "insert shared_insights"
      pattern: "shared_insights"
    - from: "src/app/api/share/[token]/route.ts"
      to: "supabase"
      via: "select shared_insights join ai_insights"
      pattern: "shared_insights.*token"
---

<objective>
Criar backend completo de links compartilháveis: tabela, criação, revogação e endpoint público.

Purpose: Usuário agency gera um link com expiração para compartilhar um insight com clientes sem expor login. O endpoint público busca os dados sem PII. Link expirado/revogado retorna resposta clara.

Output: Migration, 3 rotas de API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/app/api/insights/generations/route.ts
@src/app/api/insights/route.ts
@supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql

<interfaces>
<!-- Estrutura de ai_insights (campos relevantes) -->
```typescript
// Tabela ai_insights — campos públicos (sem PII)
{
  id: string
  insight_type: string
  title: string
  summary: string
  detailed_analysis: any
  recommendations: any[]
  confidence_score: number
  data_analyzed: any
  created_at: string
  generation_id: string
  // NÃO expor: user_id, store_id (identificadores pessoais)
}
```

<!-- Padrão de auth com service role (para endpoint público) -->
```typescript
import { createClient } from '@supabase/supabase-js'
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)
```

<!-- Padrão de auth com session (para rotas autenticadas) -->
```typescript
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — tabela shared_insights</name>
  <files>supabase/migrations/20260410200000_create_shared_insights.sql</files>
  <read_first>
    supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql
  </read_first>
  <action>
    Criar `supabase/migrations/20260410200000_create_shared_insights.sql`:

    ```sql
    -- Phase 13: Export e Compartilhamento
    -- Tabela para links compartilháveis de insights.
    -- Token é público (no URL), user_id é o dono, expires_at define validade.
    -- revoked_at NULL = ativo, preenchido = revogado.

    CREATE TABLE IF NOT EXISTS shared_insights (
      id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      token       TEXT NOT NULL UNIQUE,
      user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
      generation_id UUID NOT NULL REFERENCES insight_generation_log(id) ON DELETE CASCADE,
      expires_at  TIMESTAMPTZ NOT NULL,
      revoked_at  TIMESTAMPTZ NULL,
      created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    -- Busca rápida por token (endpoint público)
    CREATE INDEX IF NOT EXISTS idx_shared_insights_token ON shared_insights(token);
    -- Lista de links por usuário (painel de gerenciamento)
    CREATE INDEX IF NOT EXISTS idx_shared_insights_user_id ON shared_insights(user_id);

    -- RLS: usuário vê e gerencia apenas seus próprios links
    ALTER TABLE shared_insights ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "shared_insights_owner" ON shared_insights
      FOR ALL USING (auth.uid() = user_id);
    ```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS shared_insights" supabase/migrations/20260410200000_create_shared_insights.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `supabase/migrations/20260410200000_create_shared_insights.sql`
    - Contém `token TEXT NOT NULL UNIQUE`
    - Contém `expires_at TIMESTAMPTZ NOT NULL`
    - Contém `revoked_at TIMESTAMPTZ NULL`
    - Contém RLS habilitado e policy de owner
    - Contém índices em `token` e `user_id`
  </acceptance_criteria>
  <done>Tabela shared_insights criada com RLS, token único, campos de expiração e revogação</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/insights/share — criar link compartilhável (agency)</name>
  <files>src/app/api/insights/share/route.ts</files>
  <read_first>
    src/app/api/insights/generate/route.ts
    src/app/api/insights/generations/route.ts
  </read_first>
  <action>
    Criar `src/app/api/insights/share/route.ts`:

    ```typescript
    import { NextRequest, NextResponse } from 'next/server';
    import { createClient } from '@/lib/supabase/server';
    import { createClient as createServiceClient } from '@supabase/supabase-js';

    // Expiração padrão: 30 dias
    const SHARE_EXPIRY_DAYS = 30;

    export async function POST(request: NextRequest) {
      try {
        const supabase = await createClient();
        const { data: { user }, error: authError } = await supabase.auth.getUser();

        if (authError || !user) {
          return NextResponse.json({ error: 'Não autorizado' }, { status: 401 });
        }

        // Apenas agency pode compartilhar links públicos
        const { data: profile } = await supabase
          .from('user_profiles')
          .select('subscription_tier')
          .eq('id', user.id)
          .single();

        if (profile?.subscription_tier !== 'agency') {
          return NextResponse.json(
            { error: 'Links compartilháveis são exclusivos do plano Agency' },
            { status: 403 }
          );
        }

        const body = await request.json();
        const { generationId } = body;

        if (!generationId) {
          return NextResponse.json({ error: 'generationId é obrigatório' }, { status: 400 });
        }

        // Verificar que a geração pertence ao usuário
        const { data: gen, error: genError } = await supabase
          .from('insight_generation_log')
          .select('id')
          .eq('id', generationId)
          .eq('user_id', user.id)
          .eq('success', true)
          .single();

        if (genError || !gen) {
          return NextResponse.json({ error: 'Geração não encontrada' }, { status: 404 });
        }

        // Gerar token único
        const token = crypto.randomUUID().replace(/-/g, '') + crypto.randomUUID().replace(/-/g, '');

        const expiresAt = new Date();
        expiresAt.setDate(expiresAt.getDate() + SHARE_EXPIRY_DAYS);

        const serviceClient = createServiceClient(
          process.env.NEXT_PUBLIC_SUPABASE_URL!,
          process.env.SUPABASE_SERVICE_ROLE_KEY!
        );

        const { data: link, error: insertError } = await serviceClient
          .from('shared_insights')
          .insert({
            token,
            user_id: user.id,
            generation_id: generationId,
            expires_at: expiresAt.toISOString(),
          })
          .select('id, token, expires_at')
          .single();

        if (insertError || !link) {
          console.error('Erro ao criar link:', insertError);
          return NextResponse.json({ error: 'Erro ao criar link' }, { status: 500 });
        }

        return NextResponse.json({
          shareId: link.id,
          token: link.token,
          expiresAt: link.expires_at,
          shareUrl: `/share/${link.token}`,
        });
      } catch (error: any) {
        console.error('Share create error:', error);
        return NextResponse.json({ error: 'Erro interno' }, { status: 500 });
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "shared_insights\|agency\|SHARE_EXPIRY_DAYS" src/app/api/insights/share/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `src/app/api/insights/share/route.ts`
    - Contém gate `subscription_tier !== 'agency'`
    - Contém `crypto.randomUUID()` para geração do token
    - Contém `SHARE_EXPIRY_DAYS = 30`
    - Retorna `{ shareId, token, expiresAt, shareUrl }`
    - Verifica que a geração pertence ao usuário antes de criar o link
  </acceptance_criteria>
  <done>POST /api/insights/share cria link compartilhável com token único e expiração de 30 dias (agency only)</done>
</task>

<task type="auto">
  <name>Task 3: DELETE /api/insights/share/[shareId] — revogar link</name>
  <files>src/app/api/insights/share/[shareId]/route.ts</files>
  <read_first>
    src/app/api/insights/share/route.ts
  </read_first>
  <action>
    Criar `src/app/api/insights/share/[shareId]/route.ts`:

    ```typescript
    import { NextRequest, NextResponse } from 'next/server';
    import { createClient } from '@/lib/supabase/server';

    export async function DELETE(
      _request: NextRequest,
      { params }: { params: { shareId: string } }
    ) {
      try {
        const supabase = await createClient();
        const { data: { user }, error: authError } = await supabase.auth.getUser();

        if (authError || !user) {
          return NextResponse.json({ error: 'Não autorizado' }, { status: 401 });
        }

        const { shareId } = params;

        // RLS garante que só o dono pode atualizar — mas verificamos explicitamente
        const { data: updated, error } = await supabase
          .from('shared_insights')
          .update({ revoked_at: new Date().toISOString() })
          .eq('id', shareId)
          .eq('user_id', user.id)
          .is('revoked_at', null)
          .select('id')
          .single();

        if (error || !updated) {
          return NextResponse.json(
            { error: 'Link não encontrado ou já revogado' },
            { status: 404 }
          );
        }

        return NextResponse.json({ success: true, revokedId: shareId });
      } catch (error: any) {
        console.error('Share revoke error:', error);
        return NextResponse.json({ error: 'Erro interno' }, { status: 500 });
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "revoked_at\|shareId" src/app/api/insights/share/[shareId]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `src/app/api/insights/share/[shareId]/route.ts`
    - Contém `update({ revoked_at: new Date().toISOString() })`
    - Verifica `.eq('user_id', user.id)` (só dono revoga)
    - Verifica `.is('revoked_at', null)` (não revoga duas vezes)
    - Retorna `{ success: true, revokedId }` em caso de sucesso
  </acceptance_criteria>
  <done>DELETE /api/insights/share/[shareId] revoga link imediatamente, verificando propriedade</done>
</task>

<task type="auto">
  <name>Task 4: GET /api/share/[token] — endpoint público de insight</name>
  <files>src/app/api/share/[token]/route.ts</files>
  <read_first>
    src/app/api/insights/route.ts
    src/app/api/insights/share/route.ts
  </read_first>
  <action>
    Criar `src/app/api/share/[token]/route.ts`:

    ```typescript
    import { NextRequest, NextResponse } from 'next/server';
    import { createClient } from '@supabase/supabase-js';

    export async function GET(
      _request: NextRequest,
      { params }: { params: { token: string } }
    ) {
      try {
        const supabase = createClient(
          process.env.NEXT_PUBLIC_SUPABASE_URL!,
          process.env.SUPABASE_SERVICE_ROLE_KEY!
        );

        const { token } = params;

        // Buscar link pelo token
        const { data: link, error: linkError } = await supabase
          .from('shared_insights')
          .select('id, generation_id, expires_at, revoked_at')
          .eq('token', token)
          .single();

        if (linkError || !link) {
          return NextResponse.json(
            { error: 'Link não disponível', available: false },
            { status: 410 }
          );
        }

        // Verificar revogação
        if (link.revoked_at) {
          return NextResponse.json(
            { error: 'Link não disponível', available: false },
            { status: 410 }
          );
        }

        // Verificar expiração
        if (new Date(link.expires_at) < new Date()) {
          return NextResponse.json(
            { error: 'Link não disponível', available: false },
            { status: 410 }
          );
        }

        // Buscar insights da geração — apenas campos públicos (sem PII)
        const { data: insights, error: insightsError } = await supabase
          .from('ai_insights')
          .select('id, insight_type, title, summary, detailed_analysis, recommendations, confidence_score, data_analyzed, created_at')
          .eq('generation_id', link.generation_id)
          .order('created_at', { ascending: true });

        if (insightsError) {
          console.error('Erro ao buscar insights:', insightsError);
          return NextResponse.json({ error: 'Erro interno' }, { status: 500 });
        }

        return NextResponse.json({
          available: true,
          expiresAt: link.expires_at,
          insights: insights ?? [],
        });
      } catch (error: any) {
        console.error('Share fetch error:', error);
        return NextResponse.json({ error: 'Erro interno' }, { status: 500 });
      }
    }
    ```

    **Campos explicitamente excluídos (PII)**: `user_id`, `store_id`, `generation_id` (identificador interno).
    O endpoint usa service role para ler sem sessão, mas não expõe dados de identificação do usuário.
  </action>
  <verify>
    <automated>grep -c "revoked_at\|expires_at\|410\|available" src/app/api/share/[token]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `src/app/api/share/[token]/route.ts`
    - Retorna status 410 (não 404) para links inválidos/expirados/revogados
    - Retorna `{ error: 'Link não disponível', available: false }` nas 3 situações de falha
    - Select de `ai_insights` NÃO inclui `user_id` nem `store_id`
    - Verifica tanto `revoked_at` quanto `expires_at`
    - Build passa: `npm run build`
  </acceptance_criteria>
  <done>GET /api/share/[token] retorna insights públicos sem PII, com 410 para links inválidos</done>
</task>

</tasks>

<verification>
- Migration cria tabela shared_insights com RLS
- POST /api/insights/share cria link com token único (agency only)
- DELETE /api/insights/share/[shareId] revoga link (só dono)
- GET /api/share/[token] retorna insights sem PII, 410 para inválidos
- Build passa: `npm run build`
</verification>

<success_criteria>
- Backend completo de links compartilháveis com criação, revogação e acesso público
- Nenhum dado pessoal (user_id, store_id, email) exposto no endpoint público
- Link expirado ou revogado retorna 410 com mensagem clara
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-export-e-compartilhamento/13-01-SUMMARY.md`
</output>
