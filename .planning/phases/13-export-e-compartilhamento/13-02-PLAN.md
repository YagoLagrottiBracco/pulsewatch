---
phase: 13-export-e-compartilhamento
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/share/[token]/page.tsx
  - src/app/dashboard/insights/page.tsx
autonomous: true
requirements: [EXPORT-01, SHARE-01, SHARE-02, SHARE-03]

must_haves:
  truths:
    - "Página pública /share/[token] exibe insight completo sem dados pessoais do dono"
    - "Link não disponível exibe mensagem amigável (não 404 genérico)"
    - "Botão 'Exportar PDF' visível para usuários business+ na página de insights"
    - "PDF exportado contém título, resumo, recomendações e análise do insight selecionado"
    - "Botão 'Compartilhar' visível apenas para agency — abre modal com link gerado"
    - "Usuário agency pode revogar link ativo diretamente pelo modal"
  artifacts:
    - path: "src/app/share/[token]/page.tsx"
      provides: "Página pública de insight compartilhado"
      contains: "Link não disponível"
    - path: "src/app/dashboard/insights/page.tsx"
      provides: "Botões de Export PDF e Compartilhar"
      contains: "Exportar PDF"
  key_links:
    - from: "src/app/share/[token]/page.tsx"
      to: "src/app/api/share/[token]/route.ts"
      via: "fetch /api/share/{token}"
      pattern: "/api/share"
    - from: "src/app/dashboard/insights/page.tsx"
      to: "src/app/api/insights/share/route.ts"
      via: "fetch POST /api/insights/share"
      pattern: "/api/insights/share"
---

<objective>
Criar página pública de visualização de insight compartilhado e botões de Export PDF e Compartilhar na página de insights.

Purpose: Completa a experiência do usuário — agency gera o link e o cliente visualiza o insight. Usuário business+ baixa PDF. Tudo a partir da página de insights existente.

Output: Página /share/[token] e modificações em insights/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/app/dashboard/insights/page.tsx
@src/app/api/share/[token]/route.ts

<interfaces>
<!-- Resposta do GET /api/share/[token] -->
```typescript
// Sucesso:
{ available: true; expiresAt: string; insights: InsightPublic[] }

// Falha:
{ error: 'Link não disponível'; available: false }

interface InsightPublic {
  id: string
  insight_type: string
  title: string
  summary: string
  detailed_analysis: any
  recommendations: any[]
  confidence_score: number
  data_analyzed: any
  created_at: string
}
```

<!-- Resposta do POST /api/insights/share -->
```typescript
{ shareId: string; token: string; expiresAt: string; shareUrl: string }
```

<!-- Resposta do DELETE /api/insights/share/[shareId] -->
```typescript
{ success: true; revokedId: string }
```

<!-- Generation interface atual em insights/page.tsx -->
```typescript
interface Generation {
  id: string;
  generated_at: string;
  insight_count: number;
  source?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar página pública /share/[token]</name>
  <files>src/app/share/[token]/page.tsx</files>
  <read_first>
    src/app/dashboard/insights/page.tsx
    src/app/api/share/[token]/route.ts
  </read_first>
  <action>
    Criar `src/app/share/[token]/page.tsx` como Server Component (sem 'use client') que:

    1. Faz fetch do endpoint `/api/share/[token]` usando o token da URL
    2. Se `available: false`: exibe página "Link não disponível" limpa
    3. Se disponível: exibe os insights sem nenhum dado do dono (sem nome, email, store ID)

    ```tsx
    import { notFound } from 'next/navigation';

    interface InsightPublic {
      id: string;
      insight_type: string;
      title: string;
      summary: string;
      detailed_analysis: any;
      recommendations: any[];
      confidence_score: number;
      created_at: string;
    }

    async function getSharedInsights(token: string) {
      const baseUrl = process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000';
      const res = await fetch(`${baseUrl}/api/share/${token}`, { cache: 'no-store' });
      if (!res.ok) return null;
      return res.json();
    }

    export default async function SharePage({ params }: { params: { token: string } }) {
      const data = await getSharedInsights(params.token);

      if (!data || !data.available) {
        return (
          <div className="min-h-screen flex items-center justify-center bg-gray-50">
            <div className="text-center max-w-md px-6">
              <div className="text-6xl mb-4">🔒</div>
              <h1 className="text-2xl font-bold text-gray-900 mb-2">Link não disponível</h1>
              <p className="text-gray-500">
                Este link foi revogado, expirou ou não existe.
              </p>
            </div>
          </div>
        );
      }

      const insights: InsightPublic[] = data.insights;
      const expiresAt = new Date(data.expiresAt).toLocaleDateString('pt-BR');

      return (
        <div className="min-h-screen bg-gray-50 py-10 px-4">
          <div className="max-w-3xl mx-auto">
            {/* Header */}
            <div className="mb-8 text-center">
              <h1 className="text-3xl font-bold text-gray-900">Insights Compartilhados</h1>
              <p className="text-sm text-gray-400 mt-2">Válido até {expiresAt}</p>
            </div>

            {/* Insight Cards */}
            {insights.length === 0 ? (
              <p className="text-center text-gray-500">Nenhum insight encontrado nesta geração.</p>
            ) : (
              <div className="space-y-6">
                {insights.map((insight) => (
                  <div key={insight.id} className="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
                    <div className="flex items-start justify-between mb-3">
                      <h2 className="text-xl font-semibold text-gray-900">{insight.title}</h2>
                      <span className="text-xs text-gray-400 bg-gray-100 px-2 py-1 rounded-full ml-3 shrink-0">
                        {Math.round((insight.confidence_score ?? 0) * 100)}% confiança
                      </span>
                    </div>
                    <p className="text-gray-600 mb-4">{insight.summary}</p>

                    {insight.recommendations && insight.recommendations.length > 0 && (
                      <div>
                        <h3 className="text-sm font-semibold text-gray-700 mb-2">Recomendações</h3>
                        <ul className="space-y-2">
                          {insight.recommendations.map((rec: any, idx: number) => (
                            <li key={idx} className="flex items-start gap-2 text-sm text-gray-600">
                              <span className="text-blue-500 shrink-0">→</span>
                              <span>{typeof rec === 'string' ? rec : rec.action || rec.title || JSON.stringify(rec)}</span>
                            </li>
                          ))}
                        </ul>
                      </div>
                    )}
                  </div>
                ))}
              </div>
            )}

            {/* Footer neutro — sem marca ou dados do dono */}
            <p className="text-center text-xs text-gray-300 mt-10">
              Gerado por PulseWatch · {new Date().getFullYear()}
            </p>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -c "Link não disponível\|available\|/api/share" src/app/share/[token]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `src/app/share/[token]/page.tsx`
    - Contém mensagem `'Link não disponível'` para links inválidos
    - NÃO contém referências a `user_id`, `store_id`, nome do dono ou email
    - Exibe `insight.title`, `insight.summary`, `insight.recommendations`
    - Exibe data de expiração do link
    - Funciona como Server Component (sem 'use client')
  </acceptance_criteria>
  <done>Página /share/[token] exibe insight público sem PII, com fallback amigável para link inválido</done>
</task>

<task type="auto">
  <name>Task 2: Botões Export PDF e Compartilhar em insights/page.tsx</name>
  <files>src/app/dashboard/insights/page.tsx</files>
  <read_first>
    src/app/dashboard/insights/page.tsx
  </read_first>
  <action>
    Modificar `src/app/dashboard/insights/page.tsx` para adicionar dois botões na área de ações da geração selecionada. Ler o arquivo completo primeiro para entender onde ficam os botões de ação.

    **A. Adicionar estado e funções no componente:**

    ```typescript
    // Estados para compartilhamento
    const [shareLink, setShareLink] = useState<{ token: string; expiresAt: string; shareId: string } | null>(null);
    const [shareLoading, setShareLoading] = useState(false);
    const [shareError, setShareError] = useState<string | null>(null);

    // Exportar PDF — captura o elemento de insights e abre janela de impressão
    const handleExportPDF = () => {
      window.print();
    };

    // Gerar link compartilhável
    const handleShare = async () => {
      if (!selectedGenerationId) return;
      setShareLoading(true);
      setShareError(null);
      try {
        const res = await fetch('/api/insights/share', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ generationId: selectedGenerationId }),
        });
        if (!res.ok) {
          const err = await res.json();
          setShareError(err.error || 'Erro ao gerar link');
          return;
        }
        const data = await res.json();
        setShareLink({ token: data.token, expiresAt: data.expiresAt, shareId: data.shareId });
      } catch {
        setShareError('Erro ao gerar link');
      } finally {
        setShareLoading(false);
      }
    };

    // Revogar link
    const handleRevoke = async () => {
      if (!shareLink?.shareId) return;
      try {
        await fetch(`/api/insights/share/${shareLink.shareId}`, { method: 'DELETE' });
        setShareLink(null);
      } catch {
        // silencia — link será expirado naturalmente
      }
    };
    ```

    **B. Localizar onde ficam os botões de ação da geração** (área próxima ao seletor de geração, provavelmente junto ao botão "Gerar Insights"). Adicionar os dois botões logo abaixo ou ao lado do seletor:

    ```tsx
    {/* Botão Export PDF — business+ */}
    {['business', 'agency'].includes(userProfile?.subscription_tier || '') && selectedGenerationId && (
      <button
        onClick={handleExportPDF}
        className="text-sm px-3 py-1.5 rounded-lg border border-gray-200 hover:bg-gray-50 transition-colors flex items-center gap-1.5"
      >
        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
        </svg>
        Exportar PDF
      </button>
    )}

    {/* Botão Compartilhar — agency only */}
    {userProfile?.subscription_tier === 'agency' && selectedGenerationId && (
      <div className="relative">
        <button
          onClick={handleShare}
          disabled={shareLoading}
          className="text-sm px-3 py-1.5 rounded-lg border border-blue-200 text-blue-600 hover:bg-blue-50 transition-colors flex items-center gap-1.5 disabled:opacity-50"
        >
          <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" />
          </svg>
          {shareLoading ? 'Gerando...' : 'Compartilhar'}
        </button>

        {/* Painel do link gerado */}
        {shareLink && (
          <div className="absolute right-0 top-10 z-10 bg-white rounded-xl shadow-lg border border-gray-100 p-4 w-80">
            <p className="text-xs font-medium text-gray-500 mb-2">Link público (válido por 30 dias)</p>
            <div className="flex items-center gap-2 mb-3">
              <input
                readOnly
                value={`${window.location.origin}/share/${shareLink.token}`}
                className="text-xs text-gray-700 bg-gray-50 border border-gray-200 rounded-lg px-2 py-1.5 flex-1 min-w-0"
              />
              <button
                onClick={() => navigator.clipboard.writeText(`${window.location.origin}/share/${shareLink.token}`)}
                className="text-xs px-2 py-1.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shrink-0"
              >
                Copiar
              </button>
            </div>
            <button
              onClick={handleRevoke}
              className="text-xs text-red-500 hover:text-red-700 transition-colors"
            >
              Revogar link
            </button>
          </div>
        )}

        {shareError && (
          <p className="absolute right-0 top-10 text-xs text-red-500 bg-white border border-red-100 rounded-lg p-2 shadow">
            {shareError}
          </p>
        )}
      </div>
    )}
    ```

    **C. Adicionar CSS de impressão** para o PDF via `window.print()`. Adicionar uma tag `<style>` com `@media print` que esconde a sidebar, navbar, botões e seletores, deixando apenas os cards de insight:

    Procurar onde o componente renderiza (geralmente após os imports, antes do return ou dentro do JSX) e adicionar antes do return principal:

    ```tsx
    {/* Estilos de impressão para export PDF */}
    <style>{`
      @media print {
        nav, header, aside, [data-no-print] { display: none !important; }
        body { background: white; }
        .print-area { box-shadow: none !important; border: none !important; }
      }
    `}</style>
    ```

    **Importante**: Ler o arquivo completo antes de editar para entender exatamente onde fica `selectedGenerationId`, `userProfile`, e a área de botões de ação. Não quebre o layout existente — insira os botões de forma aditiva.
  </action>
  <verify>
    <automated>grep -c "Exportar PDF\|handleShare\|handleRevoke\|/api/insights/share" src/app/dashboard/insights/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/insights/page.tsx` contém `'Exportar PDF'`
    - Contém `handleExportPDF` com `window.print()`
    - Contém `handleShare` que faz POST `/api/insights/share`
    - Contém `handleRevoke` que faz DELETE `/api/insights/share/`
    - Botão "Exportar PDF" só aparece para business/agency com geração selecionada
    - Botão "Compartilhar" só aparece para agency com geração selecionada
    - Contém `@media print` para ocultar elementos de UI no PDF
    - Build passa: `npm run build`
  </acceptance_criteria>
  <done>Botões Export PDF e Compartilhar adicionados em insights/page.tsx com controle de tier e painel de link gerado</done>
</task>

</tasks>

<verification>
- /share/[token] exibe insights públicos sem PII
- Link inválido/expirado/revogado mostra "Link não disponível" (não 404)
- Botão "Exportar PDF" visível para business+ — abre diálogo de impressão do browser
- Botão "Compartilhar" visível para agency — gera link e permite revogar
- Build passa sem erros TypeScript
</verification>

<success_criteria>
- Usuário agency gera link público e cliente visualiza insight sem login (SHARE-01, SHARE-02)
- Usuário revoga link e ele fica imediatamente inacessível (SHARE-03)
- Usuário business+ baixa PDF via window.print() (EXPORT-01)
- Nenhum dado pessoal exposto na página pública
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-export-e-compartilhamento/13-02-SUMMARY.md`
</output>
