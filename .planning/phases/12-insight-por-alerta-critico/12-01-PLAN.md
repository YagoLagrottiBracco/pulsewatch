---
phase: 12-insight-por-alerta-critico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql
  - src/services/ai-insights.ts
  - src/app/api/cron/check-status/route.ts
autonomous: true
requirements: [ALERT-01, ALERT-02]

must_haves:
  truths:
    - "insight_generation_log has alert_id UUID nullable column"
    - "generateInsightsForUser accepts optional alertId param and stores it in the log"
    - "createAlertWithNotification returns alertId when alert is created"
    - "check-status cron fires insight generation async (fire-and-forget) for high/critical alerts"
    - "Deduplication: only one insight generated per user within 4 hours of alert_triggered source"
    - "check-status cron performance is not affected (no awaiting insight generation)"
  artifacts:
    - path: "supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql"
      provides: "alert_id column on insight_generation_log"
      contains: "ADD COLUMN"
    - path: "src/services/ai-insights.ts"
      provides: "generateInsightsForUser with optional alertId param"
      exports: ["generateInsightsForUser", "InsightSource"]
    - path: "src/app/api/cron/check-status/route.ts"
      provides: "Alert-triggered insight generation with dedup"
      contains: "alert_triggered"
  key_links:
    - from: "src/app/api/cron/check-status/route.ts"
      to: "src/services/ai-insights.ts"
      via: "import { generateInsightsForUser }"
      pattern: "generateInsightsForUser.*alert_triggered"
    - from: "src/services/ai-insights.ts"
      to: "supabase"
      via: "insert insight_generation_log with alert_id"
      pattern: "alert_id"
---

<objective>
Disparar geração automática de insight quando um alerta crítico é criado no cron check-status.

Purpose: Fechar o loop entre monitoramento e inteligência — quando um alerta high/critical ocorre, o sistema gera um diagnóstico de IA automaticamente via fire-and-forget, sem impactar a performance do cron. Deduplicação de 4 horas evita spam de insights.

Output: Migration, extensão de generateInsightsForUser com alertId, integração no check-status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/services/ai-insights.ts
@src/app/api/cron/check-status/route.ts
@supabase/migrations/20260410000000_add_source_column_to_insights.sql

<interfaces>
<!-- generateInsightsForUser atual (src/services/ai-insights.ts linha ~231) -->
```typescript
export type InsightSource = 'manual' | 'automatic' | 'alert_triggered';

export async function generateInsightsForUser(
  userId: string,
  source: InsightSource = 'manual'
): Promise<{ insightCount: number; generationId: string }>
```

<!-- createAlertWithNotification atual (check-status/route.ts linha 441) -->
```typescript
async function createAlertWithNotification(
  supabase: any,
  store: any,
  alertData: { type: string; severity: string; title: string; message: string; metadata?: any }
): Promise<{ alertCreated: boolean; notifications: Record<string, boolean | string> | null }>
```

<!-- Insert atual da função (linha 453) -->
```typescript
const { error } = await supabase.from('alerts').insert({
  store_id: store.id,
  ...alertData,
  is_read: false,
})
```

<!-- Pattern de dedup do weekly-insights (6 dias) — usar mesma lógica, mas 4 horas -->
```typescript
const { data: recentGen } = await supabase
  .from('insight_generation_log')
  .select('id')
  .eq('user_id', user.id)
  .gte('generated_at', cutoffTime.toISOString())
  .eq('success', true)
  .eq('source', 'alert_triggered')
  .limit(1);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — adicionar alert_id em insight_generation_log</name>
  <files>supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql</files>
  <read_first>
    supabase/migrations/20260410000000_add_source_column_to_insights.sql
    supabase/migrations/20260409000000_add_generation_id_to_ai_insights.sql
  </read_first>
  <action>
    Criar arquivo `supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql`:

    ```sql
    -- Phase 12: Insight por Alerta Crítico
    -- Adiciona alert_id em insight_generation_log para rastrear qual alerta originou o insight.
    -- Nullable: geraçoes manual e automatic nao referenciam alerta.

    ALTER TABLE insight_generation_log
      ADD COLUMN IF NOT EXISTS alert_id UUID REFERENCES alerts(id) ON DELETE SET NULL;

    -- Index para buscar geraçoes por alerta
    CREATE INDEX IF NOT EXISTS idx_insight_generation_log_alert_id
      ON insight_generation_log(alert_id)
      WHERE alert_id IS NOT NULL;
    ```
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS alert_id" supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `supabase/migrations/20260410100000_add_alert_id_to_generation_log.sql`
    - Contém `ALTER TABLE insight_generation_log ADD COLUMN IF NOT EXISTS alert_id UUID`
    - Contém `REFERENCES alerts(id) ON DELETE SET NULL`
    - Contém índice parcial com `WHERE alert_id IS NOT NULL`
    - Header de comentário referencia "Phase 12"
  </acceptance_criteria>
  <done>Migration pronta — alert_id nullable em insight_generation_log com FK para alerts</done>
</task>

<task type="auto">
  <name>Task 2: Estender generateInsightsForUser com parâmetro alertId opcional</name>
  <files>src/services/ai-insights.ts</files>
  <read_first>
    src/services/ai-insights.ts
  </read_first>
  <action>
    Modificar `generateInsightsForUser` em `src/services/ai-insights.ts`:

    **A. Atualizar a assinatura da função** — adicionar `alertId?: string` como terceiro parâmetro opcional (backward compatible — chamadores existentes não precisam mudar):

    ```typescript
    export async function generateInsightsForUser(
      userId: string,
      source: InsightSource = 'manual',
      alertId?: string
    ): Promise<{ insightCount: number; generationId: string }>
    ```

    **B. Incluir `alert_id` no insert do `insight_generation_log`** — localizar o bloco que insere no `insight_generation_log` (próximo do comentário "Log generation FIRST") e adicionar `alert_id`:

    ```typescript
    const { data: logEntry, error: logError } = await supabase
      .from('insight_generation_log')
      .insert({
        user_id: userId,
        success: true,
        source,
        ...(alertId ? { alert_id: alertId } : {}),
      })
      .select('id')
      .single();
    ```

    Não alterar nenhuma outra parte da função. Chamadores existentes (`'manual'` e `'automatic'`) continuam funcionando sem mudança.
  </action>
  <verify>
    <automated>grep -c "alertId" src/services/ai-insights.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/ai-insights.ts` função `generateInsightsForUser` aceita terceiro parâmetro `alertId?: string`
    - O insert em `insight_generation_log` inclui `alert_id: alertId` (condicional)
    - Chamadores existentes com 2 argumentos continuam válidos (TypeScript não quebra)
    - `export type InsightSource` ainda contém `'alert_triggered'`
    - Restante da função inalterado
  </acceptance_criteria>
  <done>generateInsightsForUser aceita alertId opcional e armazena no log de geração</done>
</task>

<task type="auto">
  <name>Task 3: Integrar geração por alerta no check-status cron</name>
  <files>src/app/api/cron/check-status/route.ts</files>
  <read_first>
    src/app/api/cron/check-status/route.ts
  </read_first>
  <action>
    Fazer duas modificações em `src/app/api/cron/check-status/route.ts`:

    **A. Adicionar import de `generateInsightsForUser`** no topo do arquivo (junto com outros imports):

    ```typescript
    import { generateInsightsForUser } from '@/services/ai-insights'
    ```

    **B. Modificar `createAlertWithNotification`** (linha ~441):

    1. Alterar o insert de alertas para retornar o ID criado:
    ```typescript
    // ANTES:
    const { error } = await supabase.from('alerts').insert({
      store_id: store.id,
      ...alertData,
      is_read: false,
    })

    // DEPOIS:
    const { data: alertRow, error } = await supabase
      .from('alerts')
      .insert({
        store_id: store.id,
        ...alertData,
        is_read: false,
      })
      .select('id')
      .single()
    ```

    2. Atualizar o erro check (a variável ainda é `error`):
    ```typescript
    if (error) {
      console.error('Erro ao criar alerta:', error)
      return { alertCreated: false, notifications: null }
    }
    ```

    3. Adicionar fire-and-forget APÓS o insert bem-sucedido e ANTES do bloco de notificações:
    ```typescript
    // Disparo de insight para alertas críticos (fire-and-forget — não bloqueia o cron)
    if (alertRow?.id && (alertData.severity === 'high' || alertData.severity === 'critical')) {
      triggerAlertInsight(supabase, store.user_id, alertRow.id).catch((err) =>
        console.error('Erro ao disparar insight por alerta:', err)
      )
    }
    ```

    **C. Adicionar função `triggerAlertInsight`** logo abaixo de `createAlertWithNotification` (antes de `syncShopifyProducts`):

    ```typescript
    async function triggerAlertInsight(
      supabase: any,
      userId: string,
      alertId: string
    ): Promise<void> {
      // Deduplicação: apenas 1 insight por usuário a cada 4 horas com source=alert_triggered
      const fourHoursAgo = new Date()
      fourHoursAgo.setHours(fourHoursAgo.getHours() - 4)

      const { data: recentGen } = await supabase
        .from('insight_generation_log')
        .select('id')
        .eq('user_id', userId)
        .eq('source', 'alert_triggered')
        .eq('success', true)
        .gte('generated_at', fourHoursAgo.toISOString())
        .limit(1)

      if (recentGen && recentGen.length > 0) {
        console.log(`Insight por alerta ignorado (dedup 4h) para usuário ${userId}`)
        return
      }

      await generateInsightsForUser(userId, 'alert_triggered', alertId)
      console.log(`Insight disparado por alerta ${alertId} para usuário ${userId}`)
    }
    ```

    **Atenção**: O `triggerAlertInsight` é chamado com `.catch()` (fire-and-forget), então qualquer erro interno é capturado e logado sem propagar para o cron. O cron continua normalmente.
  </action>
  <verify>
    <automated>grep -c "triggerAlertInsight\|alert_triggered\|generateInsightsForUser" src/app/api/cron/check-status/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/cron/check-status/route.ts` contém `import { generateInsightsForUser } from '@/services/ai-insights'`
    - Contém função `triggerAlertInsight`
    - `triggerAlertInsight` contém `fourHoursAgo.setHours`
    - `triggerAlertInsight` contém `.eq('source', 'alert_triggered')`
    - `createAlertWithNotification` chama `triggerAlertInsight` com `.catch()`
    - O insert de alertas usa `.select('id').single()` para retornar o ID
    - Chamadas de `createAlertWithNotification` existentes no arquivo NÃO precisam ser modificadas (return type é backward compat)
    - Build passa: `npm run build` sem erros TypeScript
  </acceptance_criteria>
  <done>check-status cron dispara insight async para alertas high/critical com dedup de 4h por usuário</done>
</task>

</tasks>

<verification>
- Migration existe com `alert_id UUID REFERENCES alerts(id)` em `insight_generation_log`
- `generateInsightsForUser` aceita `alertId?` e salva no log
- `createAlertWithNotification` retorna ID do alerta via `.select('id').single()`
- `triggerAlertInsight` implementa dedup de 4 horas e chama `generateInsightsForUser(userId, 'alert_triggered', alertId)`
- Fire-and-forget via `.catch()` — cron não bloqueia esperando insight
- Build passa: `npm run build`
</verification>

<success_criteria>
- Alertas high/critical disparam geração de insight automaticamente (ALERT-01)
- Deduplicação de 4h por usuário evita spam (ALERT-02)
- Insights gerados por alerta armazenam referência ao alerta que os originou
- Performance do check-status cron não é afetada
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-insight-por-alerta-critico/12-01-SUMMARY.md`
</output>
