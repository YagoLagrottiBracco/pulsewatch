---
phase: 12-insight-por-alerta-critico
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/dashboard/insights/page.tsx
autonomous: true
requirements: [ALERT-01]

must_haves:
  truths:
    - "Geraçoes com source='alert_triggered' exibem label 'Disparado por alerta' no seletor"
    - "formatGenerationLabel trata os três valores de source: manual, automatic, alert_triggered"
    - "Build passa sem erros TypeScript"
  artifacts:
    - path: "src/app/dashboard/insights/page.tsx"
      provides: "Label 'Disparado por alerta' para insights gerados por alerta"
      contains: "Disparado por alerta"
  key_links:
    - from: "src/app/dashboard/insights/page.tsx"
      to: "Generation interface"
      via: "source field conditional rendering"
      pattern: "alert_triggered"
---

<objective>
Mostrar "Disparado por alerta" no seletor de gerações para insights gerados por alerta crítico.

Purpose: Usuário vê claramente que o insight foi gerado automaticamente em resposta a um alerta, diferenciando de geraçoes manuais e automáticas semanais.

Output: Atualização de formatGenerationLabel em insights/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/app/dashboard/insights/page.tsx

<interfaces>
<!-- formatGenerationLabel existente (adicionado na fase 11) -->
```typescript
function formatGenerationLabel(gen: Generation): string {
  const date = new Date(gen.generated_at);
  if (gen.source === 'automatic') {
    return `Automatico — ${date.toLocaleDateString('pt-BR', { weekday: 'short', day: '2-digit', month: '2-digit', year: 'numeric' })}`;
  }
  return date.toLocaleString('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' });
}

interface Generation {
  id: string;
  generated_at: string;
  insight_count: number;
  source?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar label "Disparado por alerta" em formatGenerationLabel</name>
  <files>src/app/dashboard/insights/page.tsx</files>
  <read_first>
    src/app/dashboard/insights/page.tsx
  </read_first>
  <action>
    Localizar a função `formatGenerationLabel` em `src/app/dashboard/insights/page.tsx` e adicionar tratamento para `source === 'alert_triggered'` ANTES do check de `'automatic'`:

    ```typescript
    function formatGenerationLabel(gen: Generation): string {
      const date = new Date(gen.generated_at);
      if (gen.source === 'alert_triggered') {
        return `Disparado por alerta — ${date.toLocaleString('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' })}`;
      }
      if (gen.source === 'automatic') {
        return `Automatico — ${date.toLocaleDateString('pt-BR', { weekday: 'short', day: '2-digit', month: '2-digit', year: 'numeric' })}`;
      }
      return date.toLocaleString('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' });
    }
    ```

    Não alterar nenhuma outra parte do arquivo. Apenas substituir a função `formatGenerationLabel` existente pela versão acima.
  </action>
  <verify>
    <automated>grep -c "Disparado por alerta\|alert_triggered" src/app/dashboard/insights/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/insights/page.tsx` contém string `'Disparado por alerta'`
    - Contém `gen.source === 'alert_triggered'`
    - `formatGenerationLabel` ainda trata `'automatic'` (label "Automatico" preservado)
    - Fallback para manual inalterado
    - Build passa: `npm run build` sem erros TypeScript
  </acceptance_criteria>
  <done>Label "Disparado por alerta" exibido no seletor para insights gerados por alerta crítico</done>
</task>

</tasks>

<verification>
- `formatGenerationLabel` em page.tsx retorna "Disparado por alerta — DD/MM/YYYY HH:mm" para source='alert_triggered'
- Labels "Automatico" (phase 11) continuam funcionando
- Build passa sem erros
</verification>

<success_criteria>
- Usuário vê "Disparado por alerta — data" no seletor de gerações (ALERT-01 — identificação visual)
- Labels existentes de geração manual e automática preservados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-insight-por-alerta-critico/12-02-SUMMARY.md`
</output>
