---
phase: 14-chat-com-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260410300000_create_chat_messages.sql
  - src/app/api/insights/chat/route.ts
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "Tabela chat_messages existe com user_id, question, answer, created_at"
    - "POST /api/insights/chat retorna resposta em streaming (ReadableStream)"
    - "Rate limit aplicado: business=10/dia, agency=30/dia"
    - "Contexto real da loja injetado no prompt (alertas recentes, insights, produtos)"
    - "Resposta do contador incluída no header X-Chat-Remaining"
  artifacts:
    - path: "supabase/migrations/20260410300000_create_chat_messages.sql"
      provides: "Tabela chat_messages para histórico e rate limiting"
      contains: "CREATE TABLE chat_messages"
    - path: "src/app/api/insights/chat/route.ts"
      provides: "Endpoint de chat com streaming e rate limit"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/insights/chat/route.ts"
      to: "src/services/ai-insights.ts"
      via: "import GoogleGenerativeAI (same genAI instance pattern)"
      pattern: "generateContentStream"
    - from: "src/app/api/insights/chat/route.ts"
      to: "supabase"
      via: "insert chat_messages after stream completes"
      pattern: "chat_messages"
---

<objective>
Backend do chat: tabela para histórico/rate-limit e endpoint de streaming com contexto real da loja.

Purpose: Usuário business+ faz perguntas em linguagem natural e recebe respostas contextualizadas via Gemini streaming. Rate limit por tier impede abuso. Contexto real (alertas, insights, pedidos) garante respostas relevantes.

Output: Migration + rota POST /api/insights/chat com streaming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/services/ai-insights.ts
@src/app/api/insights/generate/route.ts
@supabase/migrations/20260410200000_create_shared_insights.sql

<interfaces>
<!-- Gemini streaming API -->
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai';
const genAI = new GoogleGenerativeAI(process.env.GOOGLE_GEMINI_API_KEY || '');
const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });
const result = await model.generateContentStream(prompt);
for await (const chunk of result.stream) {
  const text = chunk.text();
  // enqueue text to ReadableStream
}
```

<!-- Next.js streaming response -->
```typescript
return new Response(
  new ReadableStream({
    async start(controller) {
      const encoder = new TextEncoder();
      // ... stream chunks
      controller.enqueue(encoder.encode(text));
      controller.close();
    },
  }),
  {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'X-Chat-Remaining': String(remaining),
    },
  }
);
```

<!-- Rate limits por tier -->
// business: 10/dia, agency: 30/dia
```typescript
const DAILY_LIMITS: Record<string, number> = { business: 10, agency: 30 };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — tabela chat_messages</name>
  <files>supabase/migrations/20260410300000_create_chat_messages.sql</files>
  <read_first>
    supabase/migrations/20260410200000_create_shared_insights.sql
  </read_first>
  <action>
    Criar `supabase/migrations/20260410300000_create_chat_messages.sql`:

    ```sql
    -- Phase 14: Chat com Dados
    -- Tabela para histórico de mensagens de chat e rate limiting diário.
    -- answer pode ser NULL se o streaming falhar antes de completar.

    CREATE TABLE IF NOT EXISTS chat_messages (
      id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
      question    TEXT NOT NULL,
      answer      TEXT,
      created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    -- Rate limit: contar mensagens de hoje por usuário
    CREATE INDEX IF NOT EXISTS idx_chat_messages_user_date
      ON chat_messages(user_id, created_at);

    -- RLS: usuário vê apenas suas próprias mensagens
    ALTER TABLE chat_messages ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "chat_messages_owner" ON chat_messages
      FOR ALL USING (auth.uid() = user_id);
    ```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS chat_messages" supabase/migrations/20260410300000_create_chat_messages.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com `CREATE TABLE IF NOT EXISTS chat_messages`
    - Contém campos `user_id`, `question`, `answer TEXT` (nullable), `created_at`
    - Contém índice em `(user_id, created_at)` para rate limit eficiente
    - RLS habilitado com policy de owner
  </acceptance_criteria>
  <done>Tabela chat_messages criada com RLS e índice para rate limiting diário</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/insights/chat — streaming com contexto e rate limit</name>
  <files>src/app/api/insights/chat/route.ts</files>
  <read_first>
    src/services/ai-insights.ts
    src/app/api/insights/generate/route.ts
  </read_first>
  <action>
    Criar `src/app/api/insights/chat/route.ts`:

    ```typescript
    import { NextRequest } from 'next/server';
    import { createClient } from '@/lib/supabase/server';
    import { createClient as createServiceClient } from '@supabase/supabase-js';
    import { GoogleGenerativeAI } from '@google/generative-ai';

    const genAI = new GoogleGenerativeAI(process.env.GOOGLE_GEMINI_API_KEY || '');
    const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });

    const DAILY_LIMITS: Record<string, number> = { business: 10, agency: 30 };

    export async function POST(request: NextRequest) {
      const supabase = await createClient();
      const { data: { user }, error: authError } = await supabase.auth.getUser();

      if (authError || !user) {
        return new Response(JSON.stringify({ error: 'Não autorizado' }), { status: 401 });
      }

      // Tier gate — business+
      const { data: profile } = await supabase
        .from('user_profiles')
        .select('subscription_tier')
        .eq('id', user.id)
        .single();

      const tier = profile?.subscription_tier || '';
      if (!['business', 'agency'].includes(tier)) {
        return new Response(
          JSON.stringify({ error: 'Chat disponível apenas para planos Business e Agency' }),
          { status: 403 }
        );
      }

      // Rate limit: count today's messages
      const serviceClient = createServiceClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!
      );

      const todayStart = new Date();
      todayStart.setHours(0, 0, 0, 0);

      const { count } = await serviceClient
        .from('chat_messages')
        .select('id', { count: 'exact', head: true })
        .eq('user_id', user.id)
        .gte('created_at', todayStart.toISOString());

      const used = count ?? 0;
      const limit = DAILY_LIMITS[tier] ?? 10;
      const remaining = Math.max(0, limit - used);

      if (remaining === 0) {
        return new Response(
          JSON.stringify({
            error: 'Limite diário atingido',
            message: `Você atingiu o limite de ${limit} mensagens por dia. Tente novamente amanhã.`,
            remaining: 0,
            limit,
          }),
          {
            status: 429,
            headers: { 'Content-Type': 'application/json', 'X-Chat-Remaining': '0' },
          }
        );
      }

      const body = await request.json();
      const { message } = body;

      if (!message || typeof message !== 'string' || message.trim().length === 0) {
        return new Response(JSON.stringify({ error: 'message é obrigatório' }), { status: 400 });
      }

      // Fetch store context for AI prompt
      const [alertsResult, ordersResult, productsResult, insightsResult, storesResult] =
        await Promise.all([
          serviceClient
            .from('alerts')
            .select('type, severity, title, message, created_at')
            .eq('user_id', user.id)
            .order('created_at', { ascending: false })
            .limit(20),
          serviceClient
            .from('orders')
            .select('total_price, financial_status, created_at')
            .eq('user_id', user.id)
            .order('created_at', { ascending: false })
            .limit(50),
          serviceClient
            .from('products')
            .select('title, stock_quantity, price')
            .eq('user_id', user.id)
            .limit(30),
          serviceClient
            .from('ai_insights')
            .select('title, summary, insight_type, created_at')
            .eq('user_id', user.id)
            .order('created_at', { ascending: false })
            .limit(5),
          serviceClient
            .from('stores')
            .select('name, platform, url')
            .eq('user_id', user.id),
        ]);

      const contextSummary = buildContextSummary({
        alerts: alertsResult.data ?? [],
        orders: ordersResult.data ?? [],
        products: productsResult.data ?? [],
        insights: insightsResult.data ?? [],
        stores: storesResult.data ?? [],
      });

      const prompt = `Você é um assistente de análise de e-commerce. Responda em português brasileiro de forma direta e baseada nos dados reais abaixo.

=== DADOS DA LOJA ===
${contextSummary}

=== PERGUNTA DO USUÁRIO ===
${message.trim()}

Responda de forma objetiva, citando dados específicos quando relevante. Não invente dados que não estão acima.`;

      // Stream response from Gemini
      const stream = new ReadableStream({
        async start(controller) {
          const encoder = new TextEncoder();
          let fullAnswer = '';

          try {
            const result = await model.generateContentStream(prompt);
            for await (const chunk of result.stream) {
              const text = chunk.text();
              if (text) {
                fullAnswer += text;
                controller.enqueue(encoder.encode(text));
              }
            }
          } catch (err) {
            console.error('Gemini stream error:', err);
            const errorMsg = '\n\n[Erro ao gerar resposta]';
            controller.enqueue(encoder.encode(errorMsg));
            fullAnswer += errorMsg;
          } finally {
            controller.close();
            // Save message after stream completes (fire-and-forget)
            serviceClient
              .from('chat_messages')
              .insert({ user_id: user.id, question: message.trim(), answer: fullAnswer })
              .then()
              .catch((err) => console.error('Erro ao salvar chat:', err));
          }
        },
      });

      return new Response(stream, {
        headers: {
          'Content-Type': 'text/plain; charset=utf-8',
          'X-Chat-Remaining': String(remaining - 1),
          'X-Chat-Limit': String(limit),
        },
      });
    }

    function buildContextSummary(data: {
      alerts: any[];
      orders: any[];
      products: any[];
      insights: any[];
      stores: any[];
    }): string {
      const lines: string[] = [];

      if (data.stores.length > 0) {
        lines.push(`Lojas: ${data.stores.map((s) => `${s.name} (${s.platform})`).join(', ')}`);
      }

      if (data.orders.length > 0) {
        const revenue = data.orders.reduce((sum, o) => sum + (parseFloat(o.total_price) || 0), 0);
        lines.push(`Pedidos recentes: ${data.orders.length} pedidos, receita total R$${revenue.toFixed(2)}`);
      }

      if (data.products.length > 0) {
        const outOfStock = data.products.filter((p) => (p.stock_quantity ?? 0) === 0).length;
        const lowStock = data.products.filter((p) => (p.stock_quantity ?? 0) > 0 && (p.stock_quantity ?? 0) < 10).length;
        lines.push(`Produtos: ${data.products.length} cadastrados, ${outOfStock} sem estoque, ${lowStock} com estoque baixo (<10)`);
      }

      if (data.alerts.length > 0) {
        const critical = data.alerts.filter((a) => a.severity === 'critical' || a.severity === 'high');
        lines.push(`Alertas recentes: ${data.alerts.length} total, ${critical.length} críticos/altos`);
        critical.slice(0, 5).forEach((a) => {
          lines.push(`  - [${a.severity}] ${a.title}: ${a.message} (${new Date(a.created_at).toLocaleDateString('pt-BR')})`);
        });
      }

      if (data.insights.length > 0) {
        lines.push(`Insights de IA recentes:`);
        data.insights.forEach((i) => {
          lines.push(`  - ${i.title}: ${i.summary?.slice(0, 120)}...`);
        });
      }

      return lines.join('\n') || 'Nenhum dado disponível ainda.';
    }
    ```
  </action>
  <verify>
    <automated>grep -c "generateContentStream\|DAILY_LIMITS\|chat_messages\|X-Chat-Remaining" src/app/api/insights/chat/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em `src/app/api/insights/chat/route.ts`
    - Contém `DAILY_LIMITS = { business: 10, agency: 30 }`
    - Conta mensagens do dia com `.gte('created_at', todayStart.toISOString())`
    - Retorna 429 com mensagem explicativa ao atingir limite
    - Usa `model.generateContentStream(prompt)` para streaming
    - Retorna `ReadableStream` com `Content-Type: text/plain`
    - Header `X-Chat-Remaining` indica mensagens restantes
    - Contexto real (alerts, orders, products, insights) injetado no prompt
    - Salva mensagem em `chat_messages` após stream (fire-and-forget)
    - Build passa: `npm run build`
  </acceptance_criteria>
  <done>POST /api/insights/chat retorna streaming Gemini com contexto real e rate limit por tier</done>
</task>

</tasks>

<verification>
- Migration cria chat_messages com RLS e índice de rate limit
- Endpoint gatea business+ e aplica limite diário por tier
- Streaming funciona via ReadableStream + generateContentStream
- Contexto da loja (alertas, pedidos, produtos, insights) no prompt
- Build passa sem erros TypeScript
</verification>

<success_criteria>
- Chat responde com dados reais da loja via Gemini streaming (CHAT-01, CHAT-02)
- Rate limit aplicado: business 10/dia, agency 30/dia (CHAT-03)
- Header X-Chat-Remaining informa mensagens restantes (CHAT-04)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-chat-com-dados/14-01-SUMMARY.md`
</output>
