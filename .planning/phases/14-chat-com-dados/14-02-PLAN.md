---
phase: 14-chat-com-dados
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/dashboard/insights/page.tsx
autonomous: true
requirements: [CHAT-01, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "Painel de chat abre/fecha via botão toggle na página de insights"
    - "Usuário digita pergunta e recebe resposta em streaming (texto aparece progressivamente)"
    - "Contador 'X de Y mensagens restantes hoje' visível no painel"
    - "Input desabilitado quando limite atingido, com mensagem explicativa"
    - "Painel visível apenas para business+ (!upgradeRequired)"
  artifacts:
    - path: "src/app/dashboard/insights/page.tsx"
      provides: "Painel de chat com streaming, contador e controle de limite"
      contains: "Chat com Dados"
  key_links:
    - from: "src/app/dashboard/insights/page.tsx"
      to: "src/app/api/insights/chat/route.ts"
      via: "fetch POST /api/insights/chat"
      pattern: "/api/insights/chat"
---

<objective>
Painel de chat na página de insights — toggle, streaming de respostas, contador diário.

Purpose: Interface onde o usuário faz perguntas sobre os dados da loja e vê respostas chegando em tempo real. Contador visível e input bloqueado ao atingir limite.

Output: Modificações em insights/page.tsx adicionando o painel de chat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/dashboard/insights/page.tsx

<interfaces>
<!-- POST /api/insights/chat -->
// Request: { message: string }
// Response (streaming): text/plain chunks
// Headers: X-Chat-Remaining, X-Chat-Limit
// Error 429: { error, message, remaining: 0, limit }

<!-- Estados a adicionar -->
```typescript
const [chatOpen, setChatOpen] = useState(false);
const [chatMessages, setChatMessages] = useState<Array<{ role: 'user' | 'assistant'; content: string }>>([]);
const [chatInput, setChatInput] = useState('');
const [chatLoading, setChatLoading] = useState(false);
const [chatRemaining, setChatRemaining] = useState<number | null>(null);
const [chatLimit, setChatLimit] = useState<number | null>(null);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar chat panel em insights/page.tsx</name>
  <files>src/app/dashboard/insights/page.tsx</files>
  <read_first>
    src/app/dashboard/insights/page.tsx
  </read_first>
  <action>
    Ler o arquivo completo para entender a estrutura atual, depois fazer as seguintes adições:

    **A. Adicionar estados de chat** após os estados de share (Phase 13):

    ```typescript
    // Chat state (Phase 14)
    const [chatOpen, setChatOpen] = useState(false);
    const [chatMessages, setChatMessages] = useState<Array<{ role: 'user' | 'assistant'; content: string }>>([]);
    const [chatInput, setChatInput] = useState('');
    const [chatLoading, setChatLoading] = useState(false);
    const [chatRemaining, setChatRemaining] = useState<number | null>(null);
    const [chatLimit, setChatLimit] = useState<number | null>(null);
    ```

    **B. Adicionar handler `handleChatSend`** após os handlers de share:

    ```typescript
    const handleChatSend = async () => {
      const trimmed = chatInput.trim();
      if (!trimmed || chatLoading) return;

      setChatMessages((prev) => [...prev, { role: 'user', content: trimmed }]);
      setChatInput('');
      setChatLoading(true);

      // Placeholder para resposta em streaming
      setChatMessages((prev) => [...prev, { role: 'assistant', content: '' }]);

      try {
        const res = await fetch('/api/insights/chat', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ message: trimmed }),
        });

        // Atualizar contadores dos headers
        const remaining = res.headers.get('X-Chat-Remaining');
        const limit = res.headers.get('X-Chat-Limit');
        if (remaining !== null) setChatRemaining(Number(remaining));
        if (limit !== null) setChatLimit(Number(limit));

        if (res.status === 429) {
          const data = await res.json();
          setChatMessages((prev) => {
            const next = [...prev];
            next[next.length - 1] = { role: 'assistant', content: data.message || 'Limite diário atingido.' };
            return next;
          });
          setChatRemaining(0);
          return;
        }

        if (!res.ok || !res.body) {
          setChatMessages((prev) => {
            const next = [...prev];
            next[next.length - 1] = { role: 'assistant', content: 'Erro ao obter resposta.' };
            return next;
          });
          return;
        }

        // Ler stream progressivamente
        const reader = res.body.getReader();
        const decoder = new TextDecoder();
        let accumulated = '';

        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          accumulated += decoder.decode(value, { stream: true });
          const snapshot = accumulated;
          setChatMessages((prev) => {
            const next = [...prev];
            next[next.length - 1] = { role: 'assistant', content: snapshot };
            return next;
          });
        }
      } catch {
        setChatMessages((prev) => {
          const next = [...prev];
          next[next.length - 1] = { role: 'assistant', content: 'Erro de conexão. Tente novamente.' };
          return next;
        });
      } finally {
        setChatLoading(false);
      }
    };
    ```

    **C. Adicionar botão "Chat" no header** — junto aos botões de share/export (após o botão "Compartilhar"), visível para `!upgradeRequired`:

    ```tsx
    {!upgradeRequired && (
      <Button
        variant={chatOpen ? 'default' : 'outline'}
        size="sm"
        onClick={() => setChatOpen((v) => !v)}
        data-no-print
      >
        {chatOpen ? 'Fechar chat' : 'Chat com Dados'}
      </Button>
    )}
    ```

    **D. Adicionar painel de chat** no final do `return`, antes do fechamento do `<div className="container ...">`, após o grid de insights:

    ```tsx
    {/* Chat com Dados (Phase 14) */}
    {chatOpen && !upgradeRequired && (
      <div className="border rounded-xl bg-background shadow-sm flex flex-col" style={{ height: '420px' }} data-no-print>
        {/* Header do chat */}
        <div className="flex items-center justify-between px-4 py-3 border-b">
          <div className="flex items-center gap-2">
            <Sparkles className="h-4 w-4 text-primary" />
            <span className="font-medium text-sm">Chat com Dados</span>
          </div>
          {chatRemaining !== null && chatLimit !== null && (
            <span className="text-xs text-muted-foreground">
              {chatRemaining} de {chatLimit} mensagens hoje
            </span>
          )}
        </div>

        {/* Mensagens */}
        <div className="flex-1 overflow-y-auto px-4 py-3 space-y-3">
          {chatMessages.length === 0 && (
            <p className="text-sm text-muted-foreground text-center mt-8">
              Faça uma pergunta sobre os dados da sua loja.<br />
              <span className="text-xs">Ex: "Por que meu checkout falhou ontem?"</span>
            </p>
          )}
          {chatMessages.map((msg, idx) => (
            <div
              key={idx}
              className={`flex ${msg.role === 'user' ? 'justify-end' : 'justify-start'}`}
            >
              <div
                className={`max-w-[80%] rounded-xl px-3 py-2 text-sm whitespace-pre-wrap ${
                  msg.role === 'user'
                    ? 'bg-primary text-primary-foreground'
                    : 'bg-muted text-foreground'
                }`}
              >
                {msg.content || (chatLoading && idx === chatMessages.length - 1 ? '...' : '')}
              </div>
            </div>
          ))}
        </div>

        {/* Input */}
        <div className="px-4 py-3 border-t flex gap-2">
          <input
            type="text"
            value={chatInput}
            onChange={(e) => setChatInput(e.target.value)}
            onKeyDown={(e) => e.key === 'Enter' && !e.shiftKey && handleChatSend()}
            placeholder={chatRemaining === 0 ? 'Limite diário atingido' : 'Faça uma pergunta...'}
            disabled={chatLoading || chatRemaining === 0}
            className="flex-1 text-sm bg-muted rounded-lg px-3 py-2 outline-none focus:ring-1 focus:ring-primary disabled:opacity-50"
          />
          <Button
            size="sm"
            onClick={handleChatSend}
            disabled={chatLoading || !chatInput.trim() || chatRemaining === 0}
          >
            {chatLoading ? '...' : 'Enviar'}
          </Button>
        </div>
      </div>
    )}
    ```

    Localizar o fechamento correto do `<div className="flex flex-col gap-6">` para inserir o painel antes dele.
  </action>
  <verify>
    <automated>grep -c "Chat com Dados\|handleChatSend\|chatRemaining\|/api/insights/chat" src/app/dashboard/insights/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Contém estado `chatOpen`, `chatMessages`, `chatInput`, `chatLoading`, `chatRemaining`, `chatLimit`
    - Contém `handleChatSend` com leitura de stream via `reader.read()`
    - Contém botão "Chat com Dados" no header (só para `!upgradeRequired`)
    - Painel exibe contador "X de Y mensagens hoje" quando `chatRemaining !== null`
    - Input desabilitado quando `chatRemaining === 0`
    - Build passa: `npm run build`
  </acceptance_criteria>
  <done>Painel de chat com streaming, contador diário e controle de limite integrado em insights/page.tsx</done>
</task>

</tasks>

<verification>
- Botão "Chat com Dados" aparece para business+ na página de insights
- Painel abre/fecha via toggle
- Mensagens do usuário aparecem à direita, respostas à esquerda
- Texto da resposta aparece progressivamente (streaming)
- Contador mostra mensagens restantes do dia
- Input bloqueado ao atingir limite
- Build passa sem erros TypeScript
</verification>

<success_criteria>
- Usuário business+ abre chat e recebe respostas com dados reais da loja (CHAT-01)
- Contador visível de mensagens restantes (CHAT-04)
- Input desabilitado ao atingir limite diário (CHAT-03)
- Streaming sem timeout visível
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-chat-com-dados/14-02-SUMMARY.md`
</output>
