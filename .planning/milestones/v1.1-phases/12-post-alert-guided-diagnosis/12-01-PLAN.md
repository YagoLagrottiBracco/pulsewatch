---
phase: 12-post-alert-guided-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/alert-checklists.ts
  - src/components/alerts/alert-diagnosis-card.tsx
  - src/app/alerts/page.tsx
autonomous: true
requirements:
  - ALERT-01
  - ALERT-02
  - ALERT-03

must_haves:
  truths:
    - "A critical alert of type downtime shows a 'O que fazer agora - Loja offline' card with 6 checklist items"
    - "A critical alert of type stock_low shows a 'O que fazer agora - Estoque baixo / zerado' card with 6 checklist items"
    - "A critical alert of type sales_drop shows a 'O que fazer agora - Queda nas vendas' card with 7 checklist items"
    - "A non-critical alert (severity high, medium, or low) does NOT show any diagnosis card"
    - "An alert with an unmapped type does NOT show a diagnosis card and does NOT crash"
  artifacts:
    - path: "src/lib/alert-checklists.ts"
      provides: "DIAGNOSIS_CHECKLISTS constant with 3 alert type entries"
      exports: ["DIAGNOSIS_CHECKLISTS"]
    - path: "src/components/alerts/alert-diagnosis-card.tsx"
      provides: "AlertDiagnosisCard presentational component"
      exports: ["AlertDiagnosisCard"]
    - path: "src/app/alerts/page.tsx"
      provides: "Conditional rendering of AlertDiagnosisCard for critical alerts"
  key_links:
    - from: "src/app/alerts/page.tsx"
      to: "src/lib/alert-checklists.ts"
      via: "import DIAGNOSIS_CHECKLISTS"
      pattern: "DIAGNOSIS_CHECKLISTS\\[alert\\.type\\]"
    - from: "src/app/alerts/page.tsx"
      to: "src/components/alerts/alert-diagnosis-card.tsx"
      via: "import AlertDiagnosisCard"
      pattern: "<AlertDiagnosisCard"
    - from: "src/app/alerts/page.tsx"
      to: "severity gate"
      via: "conditional render"
      pattern: "alert\\.severity === 'critical'"
---

<objective>
Add contextual "O que fazer agora" diagnosis checklists to critical alerts on the /alerts page.

Purpose: When a user sees a critical alert (loja offline, estoque baixo/zerado, queda de vendas), they immediately see actionable steps to diagnose and respond — zero clicks required.

Output: One data constant file, one presentational component, one modification to the alerts page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-post-alert-guided-diagnosis/12-CONTEXT.md
@.planning/phases/12-post-alert-guided-diagnosis/12-RESEARCH.md

<interfaces>
<!-- Existing card structure from src/app/alerts/page.tsx (lines 720-793) -->
<!-- Each alert renders as: -->
<Card key={alert.id}>
  <CardHeader>
    <!-- icon, title, severity badge, "Novo" badge -->
  </CardHeader>
  <CardContent>
    <!-- store name, timestamp, email/telegram status, mark-read/delete buttons -->
  </CardContent>
  <!-- NEW: AlertDiagnosisCard inserts here, before closing </Card> -->
</Card>

<!-- Existing imports in page.tsx: -->
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { AlertTriangle, CheckCircle, Mail, MessageSquare, Trash2, Download } from 'lucide-react'

<!-- Alert object shape (from database.types.ts): -->
alert.id: string
alert.title: string
alert.message: string
alert.severity: 'low' | 'medium' | 'high' | 'critical'
alert.type: string  <!-- untyped; known values: 'downtime', 'stock_low', 'sales_drop' -->
alert.is_read: boolean
alert.stores?.name: string
alert.created_at: string
alert.email_sent: boolean
alert.telegram_sent: boolean

<!-- CardContent component from src/components/ui/card.tsx: -->
<!-- Default classes: "p-6 pt-0" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create checklist data constant and AlertDiagnosisCard component</name>
  <files>src/lib/alert-checklists.ts, src/components/alerts/alert-diagnosis-card.tsx</files>
  <read_first>
    src/components/ui/card.tsx
    src/app/alerts/page.tsx
    src/lib/database.types.ts
    .planning/phases/12-post-alert-guided-diagnosis/12-RESEARCH.md
  </read_first>
  <action>
Create two new files:

**1. `src/lib/alert-checklists.ts`** — Data constant (per D-04, D-06, D-07)

Export a `DIAGNOSIS_CHECKLISTS` constant of type `Record<string, { title: string; items: string[] }>` with exactly 3 keys:

- `downtime` with title `"O que fazer agora — Loja offline"` and 6 items:
  1. "Acesse a loja diretamente no navegador para confirmar a indisponibilidade."
  2. "Verifique o status da plataforma (painel do provedor ou status page publica)."
  3. "Cheque o DNS do dominio em uma ferramenta como MXToolbox ou DNS Checker."
  4. "Reinicie a integracao ou sincronize manualmente se a plataforma estiver disponivel."
  5. "Contate o suporte da plataforma se o problema persistir por mais de 30 minutos."
  6. "Comunique sua equipe e clientes se a indisponibilidade ultrapassar 1 hora."

- `stock_low` with title `"O que fazer agora — Estoque baixo / zerado"` and 6 items:
  1. "Identifique os SKUs afetados no painel da plataforma."
  2. "Verifique o prazo de reposicao com seu fornecedor."
  3. "Considere pausar campanhas de trafego pago para o produto afetado."
  4. "Ative a opcao 'fora de estoque' para evitar vendas de produtos indisponiveis."
  5. "Atualize a descricao ou aviso na pagina do produto se a reposicao demorar."
  6. "Registre a ocorrencia para analisar padroes de ruptura recorrente."

- `sales_drop` with title `"O que fazer agora — Queda nas vendas"` and 7 items:
  1. "Verifique se a loja esta online e o checkout esta funcionando corretamente."
  2. "Confirme se campanhas de marketing estao ativas e com orcamento disponivel."
  3. "Compare o periodo com a semana anterior para identificar sazonalidade."
  4. "Analise se houve mudanca de preco recente nos produtos principais."
  5. "Revise avaliacoes e comentarios recentes que possam indicar problema de confianca."
  6. "Cheque concorrentes diretos por promocoes que possam estar desviando trafego."
  7. "Ative um cupom ou promocao temporaria para estimular conversoes."

NOTE: Use proper Portuguese accents (acentos) in the actual file -- the items above are shown without accents only for plan encoding safety. Copy the exact text from the RESEARCH.md file which has proper accents.

**2. `src/components/alerts/alert-diagnosis-card.tsx`** — Pure presentational component (per D-01, D-03, D-05)

Create directory `src/components/alerts/` if it does not exist.

The component:
- Named export `AlertDiagnosisCard`
- Props interface: `{ title: string; items: string[] }`
- Renders a `CardContent` with `className="border-t pt-4"`
- Inside: a `div` with `className="rounded-md bg-orange-500/5 border border-orange-500/20 p-4"`
- Header row: `flex items-center gap-2 mb-3` containing a `Wrench` icon (from lucide-react, `h-4 w-4 text-orange-600`) and a `span` with `className="text-sm font-semibold text-orange-700"` displaying the title
- Body: an `ol` with `className="space-y-1.5 list-decimal list-inside"` mapping each item to an `li` with `className="text-sm text-muted-foreground"`
- Import `Wrench` from `lucide-react`
- Import `CardContent` from `@/components/ui/card`
- NO business logic, NO async calls, NO state
  </action>
  <verify>
    <automated>grep -n "DIAGNOSIS_CHECKLISTS" src/lib/alert-checklists.ts && grep -n "downtime" src/lib/alert-checklists.ts && grep -n "stock_low" src/lib/alert-checklists.ts && grep -n "sales_drop" src/lib/alert-checklists.ts && grep -n "AlertDiagnosisCard" src/components/alerts/alert-diagnosis-card.tsx && grep -n "Wrench" src/components/alerts/alert-diagnosis-card.tsx && grep -n "border-t" src/components/alerts/alert-diagnosis-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const DIAGNOSIS_CHECKLISTS" src/lib/alert-checklists.ts` returns a match
    - `grep -c "downtime\|stock_low\|sales_drop" src/lib/alert-checklists.ts` returns 3 (one key per type)
    - `grep "export function AlertDiagnosisCard" src/components/alerts/alert-diagnosis-card.tsx` returns a match
    - `grep "Wrench" src/components/alerts/alert-diagnosis-card.tsx` returns a match (icon import)
    - `grep "bg-orange-500/5" src/components/alerts/alert-diagnosis-card.tsx` returns a match
    - `grep "border-t pt-4" src/components/alerts/alert-diagnosis-card.tsx` returns a match
    - `grep "CardContent" src/components/alerts/alert-diagnosis-card.tsx` returns a match
    - `grep "title: string" src/components/alerts/alert-diagnosis-card.tsx` returns a match (props interface)
    - `grep "items: string\[\]" src/components/alerts/alert-diagnosis-card.tsx` returns a match (props interface)
    - File `src/lib/alert-checklists.ts` contains at least 4 items per checklist type
  </acceptance_criteria>
  <done>Both files exist with correct exports. DIAGNOSIS_CHECKLISTS has 3 entries (downtime, stock_low, sales_drop) with 6-7 items each. AlertDiagnosisCard is a pure presentational component using CardContent with border-t, orange-themed background, Wrench icon, and ordered list.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AlertDiagnosisCard into the alerts page for critical alerts</name>
  <files>src/app/alerts/page.tsx</files>
  <read_first>
    src/app/alerts/page.tsx
    src/lib/alert-checklists.ts
    src/components/alerts/alert-diagnosis-card.tsx
  </read_first>
  <action>
Modify `src/app/alerts/page.tsx` to render the diagnosis card inline below critical alerts (per D-01, D-02, D-03).

**Step 1: Add imports** at the top of the file (after existing imports, around line 14):
```typescript
import { DIAGNOSIS_CHECKLISTS } from '@/lib/alert-checklists'
import { AlertDiagnosisCard } from '@/components/alerts/alert-diagnosis-card'
```

**Step 2: Add conditional render** inside the `filteredAlerts.map()` loop. The current structure (lines 720-793) is:
```
<Card key={alert.id} ...>
  <CardHeader>...</CardHeader>
  <CardContent>...</CardContent>
</Card>
```

Insert the following AFTER the closing `</CardContent>` tag (line 791) and BEFORE the closing `</Card>` tag (line 792):
```tsx
{alert.severity === 'critical' && DIAGNOSIS_CHECKLISTS[alert.type] && (
  <AlertDiagnosisCard
    title={DIAGNOSIS_CHECKLISTS[alert.type].title}
    items={DIAGNOSIS_CHECKLISTS[alert.type].items}
  />
)}
```

This ensures:
- Only `severity === 'critical'` alerts show the card (per D-02)
- Only alerts with a mapped type (`downtime`, `stock_low`, `sales_drop`) show the card
- Unmapped types silently skip (no card, no crash) — per Claude's Discretion
- The card is always visible for qualifying alerts (per D-03), no toggle or accordion

**Do NOT:**
- Add any accordion, toggle, or expand/collapse mechanism (per D-01)
- Add checkboxes or interactive elements (per D-05, deferred)
- Add links inside checklist items (deferred)
- Create a new route (out of scope)
- Render the card for non-critical alerts (per D-02)
  </action>
  <verify>
    <automated>grep -n "DIAGNOSIS_CHECKLISTS" src/app/alerts/page.tsx && grep -n "AlertDiagnosisCard" src/app/alerts/page.tsx && grep -n "alert.severity === 'critical'" src/app/alerts/page.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { DIAGNOSIS_CHECKLISTS }" src/app/alerts/page.tsx` returns a match
    - `grep "import { AlertDiagnosisCard }" src/app/alerts/page.tsx` returns a match
    - `grep "alert.severity === 'critical'" src/app/alerts/page.tsx` returns a match (the gate condition)
    - `grep "DIAGNOSIS_CHECKLISTS\[alert.type\]" src/app/alerts/page.tsx` returns a match (type lookup)
    - `grep "<AlertDiagnosisCard" src/app/alerts/page.tsx` returns a match (component render)
    - `npx next build` completes without errors (exit code 0)
    - The AlertDiagnosisCard render is placed INSIDE the Card component, AFTER the existing CardContent
  </acceptance_criteria>
  <done>The alerts page imports both DIAGNOSIS_CHECKLISTS and AlertDiagnosisCard. Critical alerts with a mapped type render the diagnosis card inline. Non-critical alerts and unmapped types do not render the card. The build succeeds with no TypeScript or JSX errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Structural check:** `grep -c "DIAGNOSIS_CHECKLISTS" src/app/alerts/page.tsx src/lib/alert-checklists.ts` — both files reference it
2. **Type coverage:** `grep -c "downtime\|stock_low\|sales_drop" src/lib/alert-checklists.ts` — returns 3
3. **Gate condition:** `grep "alert.severity === 'critical'" src/app/alerts/page.tsx` — present
4. **Build:** `npx next build` — succeeds
5. **Visual (manual):** Navigate to `/alerts` in dev — critical alerts of known types show the orange-themed "O que fazer agora" card; non-critical alerts do not
</verification>

<success_criteria>
- DIAGNOSIS_CHECKLISTS constant exists with 3 alert type entries (downtime, stock_low, sales_drop), each with 5-7 actionable items in Portuguese
- AlertDiagnosisCard component renders as a CardContent with border-t separator, orange background, Wrench icon, title, and ordered list
- The alerts page conditionally renders the diagnosis card only for critical alerts with a mapped type
- Non-critical alerts show no diagnosis card
- Unmapped alert types show no diagnosis card and cause no errors
- `npx next build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-post-alert-guided-diagnosis/12-01-SUMMARY.md`
</output>
